{
  "_comment": "Copy to settings.local.json. Never commit the real file. Machine specific permissions and paths only, never credentials.",
  "permissions": {
    "allow": [
      "Bash(docker *)"
    ]
  }
}
